# TODO

- [ ] تعديل `app.py` لدعم ظهور الخلفيات/الصور عند لصق مسار محلي مثل `C:\...\png` (حفظه داخل `backgroundPath`/`img` بدل الاكتفاء بـ http).
- [ ] تعديل `app.py` في `reorder_app` لإصلاح منطق تغيير الترتيب داخل نفس الصف (تصحيح الفهرسة بعد pop/insert).
- [ ] تجربة بعد التعديل:
  - [ ] لصق رابط http لصورة والتأكد من ظهورها
  - [ ] لصق مسار محلي C:\... والتأكد من ظهورها
  - [ ] عمل Drag & Drop داخل نفس الصف والتأكد من صحة الترتيب
